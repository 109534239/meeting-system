@{
    ViewData["Title"] = "新增公告";
    var defaultToday = DateTime.Today.ToString("yyyy-MM-dd");
}

@section Styles {
    <link rel="stylesheet" href="~/css/_Layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/AdminAnnouncement.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Job_search.css" asp-append-version="true" />
}

<div class="page-header">
    <div class="page-header-inner">
        <h1>新增公告</h1>
        <p>Create Announcement</p>
    </div>
</div>

<div class="announce-form-wrap">
    <form asp-controller="AdminAnnouncement" asp-action="Create" method="post">
        <div class="form-card">

            <div class="field-item">
                <label class="field-label">標題</label>
                <input class="field-input" type="text" name="title" required />
            </div>

            <div class="field-item">
                <label class="field-label">類別</label>
                <select class="field-input" name="category">
                    <option value="最新">最新</option>
                    <option value="公告" selected>公告</option>
                    <option value="活動">活動</option>
                </select>
            </div>

            @* 🎯 1. 修正 name="sDate"，並加入 id="sDate" *@
            <div class="field-item">
                <label class="field-label">上架日期</label>
                <input class="field-input" type="date" id="sDate" name="sDate"
                       value="@defaultToday" required />
            </div>

            @* 🎯 2. 修正 name="cDate"，加入 id="cDate" 與 min 預設限制 *@
            <div class="field-item">
                <label class="field-label">下架日期</label>
                <input class="field-input" type="date" id="cDate" name="cDate"
                       value="@defaultToday" min="@defaultToday" required />
            </div>

            <div class="field-item">
                <label class="field-label">是否顯示</label>
                <select class="field-input" name="isActive">
                    <option value="true">顯示</option>
                    <option value="false">停用</option>
                </select>
            </div>

            <div class="field-item field-item--full">
                <label class="field-label">內容</label>
                <textarea class="field-input field-textarea" name="content" rows="5"></textarea>
            </div>

            <div class="form-actions">
                <a asp-controller="AdminAnnouncement" asp-action="Index" class="btn-cancel">取消</a>
                <button type="submit" class="btn-save">儲存公告</button>
            </div>

        </div>
    </form>
</div>

@* 🎯 3. 動態控制日期限制腳本 *@
@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const sDateInput = document.getElementById("sDate");
            const cDateInput = document.getElementById("cDate");

            if (sDateInput && cDateInput) {
                // 當上架日期改變時觸發
                sDateInput.addEventListener("change", function () {
                    const selectedSDate = this.value;

                    // 設定下架日期的最小值 (min) 必須大於或等於上架日期
                    cDateInput.min = selectedSDate;

                    // 若當前選取的下架日期早於新的上架日期，則自動調整下架日期為上架日期
                    if (cDateInput.value && cDateInput.value < selectedSDate) {
                        cDateInput.value = selectedSDate;
                    }
                });
            }
        });
    </script>
}